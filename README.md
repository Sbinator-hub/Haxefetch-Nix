Install process:

`git clone https://github.com/Sbinator-hub/Haxefetch-Nix.git && cd Haxefetch-Nix && nix build . && nix profile add .`

Update process inside Haxefetch-Nix folder:

`nix flake update && nix profile upgrade Haxefetch`
